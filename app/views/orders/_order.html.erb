
<tr>
	<td>
		<p><%= link_to order.id, profile_order_path(current_user, order) %></p>
	</td>
	<td>
		<p><%= link_to order.created_at, profile_order_path(current_user, order) %></p>
	</td>
	<td>
		<p><%= link_to order.items.length, profile_order_path(current_user, order) %></p>
	</td>
	<td>
		<p><%= link_to "#{order.subtotal}€", profile_order_path(current_user, order) %></p>
	</td>
</tr>

